# Core dependencies
streamlit>=1.32.0
torch>=2.0.0
numpy>=1.20.0
pandas>=1.3.0
pydub>=0.25.1
ffmpeg-python>=0.2.0

# Whisper dependencies
openai-whisper>=20231117

# Voxtral dependencies
transformers>=4.51.3
accelerate>=0.20.0
sentencepiece>=0.2.0
mistral-common
# NeMo dependencies (Parakeet and Canary-Qwen)
# Note: Canary-Qwen requires latest NeMo trunk version
# Install with: python -m pip install "nemo_toolkit[asr,tts] @ git+https://github.com/NVIDIA/NeMo.git"
nemo_toolkit[asr]

