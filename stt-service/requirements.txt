# STT Service Requirements

# Install numpy first (required by many packages)
numpy>=1.20.0

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
torch>=2.0.0
torchaudio>=2.9.0
soundfile>=0.13.1
pandas>=1.3.0
pydub>=0.25.1
ffmpeg-python>=0.2.0
packaging>=21.0
httpx>=0.24.0

# Whisper dependencies
openai-whisper>=20240930

# NeMo dependencies (Parakeet and Canary-Qwen)
# Note: NeMo will install sox which requires numpy to be already installed
nemo_toolkit[asr] @ git+https://github.com/NVIDIA/NeMo.git
megatron-core
sacrebleu
cuda-python>=12.3
sentencepiece>=0.2.0
mistral-common
fasttext-wheel
